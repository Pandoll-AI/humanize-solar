---
name: humanize-solar
description: 한국어 원고를 Solar(Upstage) API로 윤문한다. 문장을 쪼개지 않고 어휘·어순으로 읽기를 개선하며, 코드블록·표·제목·수치는 건드리지 않는다. 트리거 — "solar로 윤문", "humanize-solar", "솔라로 다듬어줘", "업스테이지로 윤문". 범용 한국어 글쓰기 요청에는 발동하지 않는다.
---

# humanize-solar

한국어 원고를 Solar에 보내 다듬는다. 하는 일은 하나다 — **어색한 표현, 늘어지는 문장, 굳어 있는 명사 표현을 어휘와 어순으로 고친다.** 문장을 쪼개지 않고, 코드블록·표·제목·리스트·수치·고유명사는 손대지 않는다.

## 사전 준비

API 키가 환경변수에 있어야 한다.

```bash
export UPSTAGE_API_KEY="up_..."
```

키가 없으면 스크립트가 종료 코드 2와 함께 안내를 내보낸다. 키 발급 경로는 README를 참고한다.

## 호출

```bash
python3 scripts/humanize.py 원고.md              # 결과를 stdout으로
python3 scripts/humanize.py 원고.md -o 결과.md
cat 원고.md | python3 scripts/humanize.py
python3 scripts/humanize.py 원고.md --effort high
python3 scripts/humanize.py 원고.md --dry-run    # 보낼 페이로드만 확인, 호출 없음
```

결과는 stdout, 진행과 경고는 stderr로 나간다. 파이프로 이어 붙여도 오염되지 않는다.

원고를 손보기 전에 `--dry-run`으로 무엇이 실제 윤문 대상인지 확인하는 편이 안전하다. 보호 구역으로 잡힌 부분은 페이로드에 아예 들어가지 않는다.

## reasoning effort

기본값은 `medium`이다. 올린다고 더 많이 고치지는 않는다 — `medium`에서 `xhigh`로 올렸을 때 변경률은 2.12%에서 2.20%로 사실상 그대로였다. 다만 표기 정확도는 올라간다. 같은 실험에서 곡선따옴표 손실 8개가 전부 복구됐다.

**추론을 더 태우면 더 고치는 게 아니라 덜 망가뜨린다.** 편집량이 부족해 보인다고 effort를 올리는 것은 잘못된 처방이다.

## 프롬프트를 고치지 마라

스크립트 안의 프롬프트는 9라운드 실측으로 확정된 것이다. **여기에 금지 조항을 추가하면 결과가 무너진다.**

"문장부호를 바꾸지 마라"라는 한 줄을 더했을 때 변경률이 4.34%에서 0.80%로 떨어졌다. 조항 하나를 지키게 하려다 편집 자체가 붕괴했다. 반대로 "끊지 마라"라는 경고를 **지우자** 과분할이 사라졌다(문장 분할비 1.15배 → 1.00배). 곡선따옴표도 "바꾸지 마라"라고 말했을 때 22개 중 14개만 남았고, 아무 말도 하지 않으니 22개 전부 보존됐다.

금지어는 그 행동을 막는 게 아니라 **오히려 촉발한다.** 무언가를 지키게 하고 싶으면 프롬프트에 조항을 더하지 말고, 그 항목을 보호 구역 판정에 넣거나 후처리로 검사하는 편이 낫다.

## 실패했을 때

**블록 파싱 실패** — 모델이 `[[번호]]` 형식을 어기면 그 청크만 3회까지 재시도하고, 그래도 실패하면 원문을 그대로 두고 stderr에 경고한다. 나머지 청크는 계속 처리된다. 결과물에 손대지 않은 문단이 섞여 있을 수 있으므로, 경고가 나오면 사용자에게 어느 부분이 원문인지 알린다.

**응답 잘림** — `finish_reason`이 `length`면 해당 청크를 원문으로 되돌리고 경고한다. `--max-tokens`를 올리거나 `--effort`를 낮춰 다시 시도한다. Solar는 추론 토큰을 함께 소모하므로 effort가 높을수록 잘릴 여지가 커진다.

**레이트리밋** — 429는 지수 백오프로 최대 4회 재시도한다. 그래도 실패하면 종료 코드 1로 끝난다. 잠시 뒤 다시 돌린다.

어느 경우든 **실패를 감추고 결과만 보고하지 않는다.** 무엇이 처리되지 않았는지 그대로 전한다.

## 알아둘 것

변경률이 높다고 좋은 윤문이라는 보장은 없다. 결과는 사람이 읽고 판단해야 한다.

원고에 따라 개입 폭이 크게 다르다. 학술·보고문 계열은 명사화 표현이 많아 손댈 곳이 많고, 이미 잘 쓰인 구어체 원고는 거의 그대로 통과한다. 손대지 않는 것도 정상적인 결과다.
